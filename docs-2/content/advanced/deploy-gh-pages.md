## Base paths need real coverage

Deploying under a subpath changes more than links.

It affects canonical URLs, hreflang output, root redirects, localized routing, and the preview story.

## Treat subpath deploys as first-class

If a framework says it supports GitHub Pages, it should prove that support with explicit build and preview coverage instead of assuming `/`.
