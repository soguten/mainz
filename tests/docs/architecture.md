# Test Architecture

Principles:

- `core`: shared contracts with build reuse
- `special`: cases with a special setup, profile, or artifact shape
- `smoke`: lean verification against real apps
- `checks`: reusable domain diagnostics
- `helpers`: shared infrastructure, build context, and utilities
- `fixtures`: intentional surfaces for validating framework behavior
