export interface ConceptCardData {
  title: string;
  description: string;
  tag: string;
}

export interface StageData {
  label: string;
  description: string;
}

export interface NextStepCardData {
  title: string;
  description: string;
}
